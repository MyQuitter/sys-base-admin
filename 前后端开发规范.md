# 基础管理系统 - 前后端开发规范

> 版本：v1.1 | 适用项目：NestJS 后端 + React 管理后台 | 关联文档：[技术方案文档](./技术方案文档.md)

本文档为全栈日常开发的**唯一编码约束来源**，涵盖通用约定、后端（NestJS）、前端（React + Ant Design）及联调协作规范。功能清单、页面规划、排期见 [技术方案文档](./技术方案文档.md)。

---

## 1. 仓库结构

推荐 Monorepo 布局（`server/` + `admin-web/` 同仓）：

```
/
├── server/                 # NestJS 后端
│   ├── src/
│   ├── package.json
│   └── ...
├── admin-web/              # React 管理后台
│   ├── src/
│   ├── package.json
│   └── ...
├── docker-compose.yml
├── .editorconfig
└── README.md
```

| 子项目 | 技术栈 | 默认端口 |
|--------|--------|----------|
| `server/` | NestJS 10 + TypeORM + MySQL + Redis | 3000 |
| `admin-web/` | React 18 + Vite 5 + Ant Design 5 | 5173 |

技术选型详情见 [技术方案文档 §1.2–§1.3](./技术方案文档.md#12-技术栈)。实施前置决策（Token、Monorepo、菜单策略等）见 [技术方案文档 §1.4](./技术方案文档.md#14-实施前置决策已确定)。

### 1.1 实施前置决策（摘要）

| 决策项 | 选定方案 |
|--------|----------|
| 仓库布局 | Monorepo：`server/` + `admin-web/` |
| Access Token | 前端 `sessionStorage`，Header 携带 |
| Refresh Token | 后端 `HttpOnly` Cookie，前端不存 Store |
| 菜单路由 | 后端 `GET /api/menus/tree` 驱动 |
| API JSON 字段 | camelCase（数据库列 snake_case） |

---

## 2. 通用规范

### 2.1 Git 分支与提交

**分支：**

| 分支 | 用途 |
|------|------|
| `main` | 生产环境 |
| `develop` | 开发集成 |
| `feature/{name}` | 功能开发 |
| `bugfix/{name}` | Bug 修复 |

**提交格式（Husky + commitlint 强制）：**

```
<type>(<scope>): <description>
```

| type | 含义 | scope 示例 |
|------|------|------------|
| feat | 新功能 | `user`、`auth`、`admin-web` |
| fix | 修复 | `menu`、`login` |
| docs | 文档 | - |
| refactor | 重构 | - |
| test | 测试 | - |
| chore | 构建/工具 | - |

```
feat(user): 添加用户重置密码接口

- 后端 POST /api/users/:id/reset-password
- 前端用户列表增加重置密码按钮
```

### 2.2 接口协作约定（前后端共同遵守）

#### 基础约定

- **Base URL**：`/api`（后端 `setGlobalPrefix('api')`，前端 `VITE_API_BASE_URL`）
- **数据格式**：JSON，`Content-Type: application/json`
- **认证头**：`Authorization: Bearer <accessToken>`
- **时间戳**：毫秒级 Unix 时间戳

#### 统一响应结构

**成功：**

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1699999999999
}
```

**失败：**

```json
{
  "code": 400,
  "message": "参数错误",
  "errorCode": "VALIDATION_FAILED",
  "detail": "用户名不能为空",
  "timestamp": 1699999999999
}
```

- HTTP 状态码与 body `code` **保持一致**
- 业务子码使用 `errorCode`（如 `USER_NOT_FOUND`）
- 分页列表 `data` 结构：`{ items, total, page, pageSize }`

#### 分页查询参数（前后端统一）

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| `page` | number | 1 | 从 1 开始 |
| `pageSize` | number | 10 | 上限 100 |
| `sortBy` | string | `created_at` | 须在白名单内 |
| `sortOrder` | `ASC` / `DESC` | `DESC` | 排序方向 |

#### RESTful 路由

| 操作 | 方法 | 路径 |
|------|------|------|
| 列表 | GET | `/api/{resource}` |
| 详情 | GET | `/api/{resource}/{id}` |
| 创建 | POST | `/api/{resource}` |
| 全量更新 | PUT | `/api/{resource}/{id}` |
| 部分更新 | PATCH | `/api/{resource}/{id}` |
| 删除 | DELETE | `/api/{resource}/{id}` |
| 树形 | GET | `/api/{resource}/tree` |

完整接口清单见 [技术方案文档 §7](./技术方案文档.md#7-api-接口清单)。

### 2.3 权限码约定

格式：`{模块}:{操作}`

| 权限码 | 说明 |
|--------|------|
| `user:list` | 用户列表 |
| `user:create` | 创建用户 |
| `user:update` | 编辑用户 |
| `user:delete` | 删除用户 |
| `role:assign` | 分配角色权限 |

- **后端**：`@RequirePermissions('user:create')` 校验
- **前端**：`AuthButton permission="user:create"` 控制按钮显隐
- **菜单**：菜单节点绑定权限码，无权限则不展示

### 2.4 环境变量

**后端 `server/.env.development`：**

```bash
PORT=3000
DB_HOST=localhost
JWT_SECRET=your-secret-key-at-least-32-chars
CORS_ORIGINS=http://localhost:5173
```

**前端 `admin-web/.env.development`：**

```bash
VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_TITLE=基础管理系统
```

禁止将 `.env.production` 中的密钥提交至 Git。

### 2.5 代码风格（全栈统一）

- 缩进 2 空格、单引号、行宽 120、末尾分号
- 配置文件：根目录 `.editorconfig` + `.prettierrc` 两端共用
- ESLint：后端 `server/eslint.config.mjs`，前端 `admin-web/eslint.config.mjs`（规则一致）

---

## 3. 后端开发规范（NestJS）

### 3.1 目录结构

```
server/src/
├── common/
│   ├── filters/
│   ├── interceptors/
│   ├── guards/
│   ├── decorators/
│   ├── utils/
│   └── exceptions/
├── modules/
│   ├── base/             # 基础管理系统域（auth、user、role 等）
│   │   ├── base.module.ts
│   │   └── {子模块}/
│   └── {feature}/        # 未来功能域，与 base 平级
├── config/
├── entities/
│   └── base.entity.ts
└── main.ts
```

每个子模块包含：`*.module.ts`、`*.controller.ts`、`*.service.ts`、`dto/`、`entities/`。`AppModule` 仅 import 各域聚合模块（如 `BaseModule`），不直接 import 子模块。

### 3.2 TypeScript 规范

- 对象结构用 `interface`，联合/交叉类型用 `type`
- 禁止 `any`（警告级 ESLint 规则）
- 接口名不加 `I` 前缀

| 类型 | 规范 | 示例 |
|------|------|------|
| 类名 | PascalCase | `UserService` |
| 方法/变量 | camelCase | `findUserById` |
| 常量 | UPPER_SNAKE_CASE | `MAX_PAGE_SIZE` |
| 文件 | kebab-case | `user.service.ts` |

**导入顺序：** `@nestjs/*` → 第三方 → 项目内部 → 同目录相对路径。

### 3.3 应用启动（main.ts）

```typescript
app.setGlobalPrefix('api');
app.use(helmet());
app.enableCors({
  origin: process.env.CORS_ORIGINS?.split(',') ?? [],
  credentials: true, // 允许前端携带 Refresh Cookie
});
app.useGlobalPipes(new ValidationPipe({
  whitelist: true,
  forbidNonWhitelisted: true,
  transform: true,
  transformOptions: { enableImplicitConversion: true },
}));
app.useGlobalFilters(new HttpExceptionFilter());
app.useGlobalInterceptors(new ResponseInterceptor(), new LoggingInterceptor());
```

### 3.4 Controller 规范

- RESTful 复数路由：`@Controller('users')`
- 静态路由放在动态路由之前
- 使用 `ParseIntPipe`，禁止 `+id` 强转
- Controller 不含业务逻辑，不写数据库操作

```typescript
@Get(':id')
findOne(@Param('id', ParseIntPipe) id: number) {
  return this.userService.findOne(id);
}
```

### 3.5 Service 规范

- 单一职责，构造函数注入依赖
- 业务异常抛 `BusinessException`
- 多表写操作使用 `DataSource.transaction()`

```typescript
async createUser(dto: CreateUserDto): Promise<User> {
  return this.dataSource.transaction(async (manager) => {
    const user = manager.create(User, dto);
    return manager.save(user);
  });
}
```

### 3.6 DTO 规范

```typescript
export class CreateUserDto {
  @IsNotEmpty() @IsString() username: string;
  @IsNotEmpty() @IsString() password: string;
}

export class UpdateUserDto extends PartialType(CreateUserDto) {}

export class QueryUserDto {
  @IsOptional() @Type(() => Number) @Min(1) page?: number = 1;
  @IsOptional() @Type(() => Number) @Min(1) @Max(100) pageSize?: number = 10;
}
```

- 所有入参必须 DTO + 校验装饰器
- Swagger：`@ApiTags`、`@ApiOperation`、`@ApiProperty`

### 3.7 数据库规范

**实体基类（所有业务实体必须继承）：**

```typescript
export abstract class BaseEntity {
  @PrimaryGeneratedColumn() id: number;
  @CreateDateColumn({ name: 'created_at' }) createdAt: Date;
  @UpdateDateColumn({ name: 'updated_at' }) updatedAt: Date;
  @DeleteDateColumn({ name: 'deleted_at' }) deletedAt?: Date;
}
```

- 表名：`sys_` 前缀 + snake_case（如 `sys_user`）
- 列名：snake_case；TypeScript 属性：camelCase
- 禁止生产环境 `synchronize: true`
- 变更走迁移：`npm run migration:generate` → `migration:run`

### 3.8 RBAC

```typescript
@Public()
@Post('login')
login(@Body() dto: LoginDto) { ... }

@RequirePermissions('user:create')
@Post()
create(@Body() dto: CreateUserDto) { ... }
```

Guard 顺序：`JwtAuthGuard` → `PermissionsGuard`。

### 3.9 安全要点

- JWT：Access 15min（body 返回）；Refresh 7d（`HttpOnly` Cookie + Redis 校验）
- 登录/刷新接口：`Set-Cookie` 写入 Refresh；登出时清除 Cookie 与 Redis 记录
- CORS：`credentials: true`，`origin` 白名单包含前端地址
- 密码：bcrypt rounds=10
- 参数化查询，禁止 SQL 拼接
- `@nestjs/throttler` 限流
- 日志脱敏：不记录密码、Token

### 3.10 后端测试

| 类型 | 文件 | 覆盖率 |
|------|------|--------|
| 单元测试 | `*.spec.ts` | Service ≥80% |
| E2E | `*.e2e-spec.ts` | 关键 API ≥40% |

```typescript
beforeEach(async () => {
  const moduleRef = await Test.createTestingModule({ ... }).compile();
  // ...
});
```

---

## 4. 前端开发规范（React + Ant Design）

### 4.1 目录结构

```
admin-web/src/
├── api/                   # 按模块封装请求，禁止在组件内直接 axios
│   ├── auth.ts
│   └── user.ts
├── components/            # 通用组件（不含业务逻辑）
│   ├── AuthButton/
│   └── PageTable/
├── layouts/
│   └── BasicLayout.tsx
├── pages/                 # 页面（按模块分子目录）
│   ├── login/
│   ├── system/user/
│   └── monitor/
├── router/
│   ├── routes.tsx
│   └── guard.tsx
├── stores/
│   ├── useAuthStore.ts
│   └── useMenuStore.ts
├── types/                 # 与后端响应对应的 TS 类型
│   └── api.d.ts
├── utils/
│   ├── request.ts
│   └── permission.ts
├── App.tsx
└── main.tsx
```

页面规划见 [技术方案文档 §2.5](./技术方案文档.md#25-后台管理界面admin-ui)。

### 4.2 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 组件文件 | PascalCase | `UserList.tsx` |
| 组件名 | 与文件名一致 | `UserList` |
| Hook | camelCase，`use` 前缀 | `useAuthStore` |
| API 函数 | camelCase，动词开头 | `getUserList`、`createUser` |
| 类型/接口 | PascalCase | `UserItem`、`ApiResponse` |
| 常量 | UPPER_SNAKE_CASE | `TOKEN_KEY` |
| 样式模块 | `*.module.css` 或 CSS-in-JS | `UserList.module.css` |

### 4.3 组件规范

#### 分层原则

| 层级 | 职责 | 示例 |
|------|------|------|
| `pages/` | 页面编排、组合业务组件 | `UserList.tsx` |
| `components/` | 可复用 UI，无具体业务 API | `PageTable`、`AuthButton` |
| `api/` | HTTP 请求封装 | `user.ts` |
| `stores/` | 全局状态（Token、菜单、用户） | `useAuthStore` |

#### 组件编写

- 使用函数组件 + Hooks，不使用 Class 组件
- 单文件超过 300 行应拆分子组件或自定义 Hook
- Props 必须定义 TypeScript 接口
- **页面与组件均禁止直接调用 `axios`**，统一通过 `api/` 层访问后端

```typescript
interface UserListProps {
  departmentId?: number;
}

export const UserList: React.FC<UserListProps> = ({ departmentId }) => {
  // ...
};
```

### 4.4 路由规范

- 使用 React Router 6，`createBrowserRouter` 或 `<Routes>`
- 路由分**静态**（login、403、404）和**动态**（登录后按菜单注册）
- 路由 `path` 与菜单 `path` 字段保持一致（见技术方案 §13.2）

```typescript
// router/guard.tsx
export function AuthGuard({ children }: { children: React.ReactNode }) {
  const token = useAuthStore((s) => s.accessToken);
  if (!token) return <Navigate to="/login" replace />;
  return <>{children}</>;
}
```

### 4.5 状态管理（Zustand）

**仅存全局必要状态：**

| Store | 内容 |
|-------|------|
| `useAuthStore` | `accessToken`、`userInfo`、`permissions`、`setAuth()`、`logout()` |
| `useMenuStore` | 动态菜单树、`fetchMenus()` |

- Refresh Token 由后端 `HttpOnly` Cookie 管理，**不进入** Zustand / localStorage
- 页面级状态用 `useState` / `useReducer`，不上提至全局 Store
- Access Token 读写封装在 Store action 内，禁止组件直接操作 `sessionStorage`

```typescript
interface AuthState {
  accessToken: string | null;
  userInfo: UserInfo | null;
  setAuth: (token: string, user: UserInfo) => void;
  logout: () => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  accessToken: sessionStorage.getItem('accessToken'),
  userInfo: null,
  setAuth: (accessToken, userInfo) => {
    sessionStorage.setItem('accessToken', accessToken);
    set({ accessToken, userInfo });
  },
  logout: () => {
    sessionStorage.removeItem('accessToken');
    set({ accessToken: null, userInfo: null });
  },
}));
```

### 4.6 API 请求封装

所有请求通过 `utils/request.ts` 导出的实例：

```typescript
// utils/request.ts
import { message as antdMessage } from 'antd';
import axios from 'axios';
import { useAuthStore } from '@/stores/useAuthStore';

const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 15000,
  withCredentials: true, // Refresh Token Cookie 跨域携带
});

request.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

request.interceptors.response.use(
  (res) => {
    const { code, data, message: msg } = res.data;
    if (code === 200) return data;
    antdMessage.error(msg);
    return Promise.reject(new Error(msg));
  },
  async (error) => {
    if (error.response?.status === 401 && !error.config?._retry) {
      // 调用 refresh（Cookie 自动携带），成功后重试原请求；失败则 logout
    }
    return Promise.reject(error);
  },
);

export default request;
```

**api/ + types/ 配套示例：**

```typescript
// types/api.d.ts
export interface PageResult<T> {
  items: T[];
  total: number;
  page: number;
  pageSize: number;
}

export interface UserItem {
  id: number;
  username: string;
  nickname?: string;
  status: number;
  createdAt: string;
}

export interface QueryUserParams {
  page?: number;
  pageSize?: number;
  username?: string;
}
```

```typescript
// api/user.ts
import request from '@/utils/request';
import type { PageResult, QueryUserParams, UserItem } from '@/types/api';

export function getUserList(params: QueryUserParams) {
  return request.get<never, PageResult<UserItem>>('/users', { params });
}

export function createUser(data: Pick<UserItem, 'username' | 'nickname'>) {
  return request.post<never, UserItem>('/users', data);
}
```

### 4.7 类型定义（与后端对齐）

拦截器已解包 `data`，`api/` 函数返回业务数据类型即可。通用包装类型：

```typescript
export interface ApiResponse<T = unknown> {
  code: number;
  message: string;
  data: T;
  timestamp: number;
}
```

- 响应字段统一 **camelCase**；数据库列 snake_case 由后端转换
- 完整示例见 §4.6 `types/api.d.ts` + `api/user.ts`

### 4.8 列表页 CRUD 模板

所有管理页遵循统一交互模式：

1. **顶部**：`Form` 搜索区（`layout="inline"`），查询 / 重置按钮
2. **操作栏**：「新增」等按钮包在 `<AuthButton permission="user:create">` 内
3. **中部**：`Table` + 后端分页（`pagination={{ current, pageSize, total, onChange }}`）
4. **弹窗**：`Modal` + `Form` 完成新增/编辑，成功后 `reload` 表格

```typescript
export default function UserListPage() {
  const [loading, setLoading] = useState(false);
  const [data, setData] = useState<PageResult<UserItem>>();
  const [query, setQuery] = useState<QueryUserParams>({ page: 1, pageSize: 10 });

  const loadData = useCallback(async () => {
    setLoading(true);
    try {
      const res = await getUserList(query);
      setData(res);
    } finally {
      setLoading(false);
    }
  }, [query]);

  useEffect(() => { loadData(); }, [loadData]);

  return (
  <>
    <SearchForm onSearch={setQuery} />
    <AuthButton permission="user:create" type="primary" onClick={openCreate}>
      新增用户
    </AuthButton>
    <Table
      rowKey="id"
      loading={loading}
      dataSource={data?.items}
      pagination={{
        current: query.page,
        pageSize: query.pageSize,
        total: data?.total,
        onChange: (page, pageSize) => setQuery((q) => ({ ...q, page, pageSize })),
      }}
    />
  </>
  );
}
```

### 4.9 Ant Design 使用约定

- 优先使用 Ant Design 5 组件，不重复造轮子
- 表单：`Form` + `rules` 校验，提交前 `form.validateFields()`
- 消息提示：成功 `message.success`，失败由拦截器统一 `message.error`
- 确认删除：`Modal.confirm` 二次确认
- 图标：`@ant-design/icons`，菜单图标存后端菜单配置
- 主题：在 `ConfigProvider` 统一配置 `token`（主色、圆角等）

### 4.10 权限组件

```typescript
// components/AuthButton/index.tsx
interface AuthButtonProps extends ButtonProps {
  permission: string;
}

export const AuthButton: React.FC<AuthButtonProps> = ({ permission, children, ...rest }) => {
  const permissions = useAuthStore((s) => s.userInfo?.permissions ?? []);
  if (!permissions.includes(permission)) return null;
  return <Button {...rest}>{children}</Button>;
};
```

### 4.11 前端测试

| 类型 | 工具 | 范围 |
|------|------|------|
| 单元测试 | Vitest | `utils/`、`hooks/`、纯函数组件 |
| 组件测试 | Testing Library | 通用组件交互 |
| E2E（可选） | Playwright | 登录、核心 CRUD 流程 |

```bash
# admin-web/
npm run test          # Vitest
npm run test:coverage
```

---

## 5. 联调规范

### 5.1 开发联调流程

1. 后端接口开发完成并在 Swagger 自测通过
2. 前端在 `api/` 层对接，使用 Mock 数据并行开发（可选 MSW）
3. 联调时前端 `VITE_API_BASE_URL` 指向本地后端
4. 权限联调：确认菜单树、按钮权限码与后端 `@RequirePermissions` 一致

### 5.2 Token 刷新约定

| 场景 | 后端 | 前端 |
|------|------|------|
| 登录成功 | body 返回 `accessToken`；`Set-Cookie` 写 Refresh（HttpOnly） | Store 存 accessToken；`withCredentials: true` |
| Access 过期 | 返回 401 | `POST /api/auth/refresh`（Cookie 自动携带），成功后重试 |
| Refresh 失效 | 返回 401 | `logout()`，跳转 `/login` |

### 5.3 错误处理分工

| 错误 | 后端 | 前端 |
|------|------|------|
| 参数校验 400 | `detail` 列出字段错误 | 表单字段标红或 `message.error` |
| 未登录 401 | 统一异常格式 | 拦截器处理刷新或登出 |
| 无权限 403 | `errorCode: FORBIDDEN` | 跳转 `/403` 或隐藏操作 |
| 服务器 500 | 不暴露堆栈 | `message.error('服务异常')` + 可选上报 |

### 5.4 接口变更流程

1. 后端先更新 Swagger 文档与 DTO
2. 在 PR 描述中标注**破坏性变更**
3. 前端同步更新 `types/` 与 `api/`
4. 涉及权限码变更时，同步更新菜单种子数据

---

## 6. 开发流程与部署

### 6.1 CI 流水线（各子项目独立或 Monorepo 统一）

```
lint → test → build → e2e（可选）
```

### 6.2 本地启动

```bash
# 基础设施
docker-compose up -d

# 后端
cd server && npm install && npm run start:dev

# 前端
cd admin-web && npm install && npm run dev
```

### 6.3 生产部署

- **后端**：多阶段 Docker 构建，`node:20-alpine`，暴露 3000
- **前端**：`npm run build` 产出 `dist/`，Nginx 托管静态资源，`/api` 反向代理至后端

```nginx
location /api {
    proxy_pass http://127.0.0.1:3000;
}
location / {
    try_files $uri $uri/ /index.html;
}
```

### 6.4 健康检查

- **接口**：`GET /api/health`（`@Public()` 跳过认证）
- **检查项**：MySQL、Redis 连接、进程存活

---

## 7. 工具链配置

### 7.1 .editorconfig（仓库根目录，前后端共用）

```ini
root = true

[*]
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = lf
trim_trailing_whitespace = true
insert_final_newline = true
```

### 7.2 .prettierrc（前后端共用）

```json
{
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 120,
  "tabWidth": 2,
  "semi": true
}
```

### 7.3 ESLint（后端 server/eslint.config.mjs）

```javascript
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommendedTypeChecked,
  {
    languageOptions: {
      parserOptions: { project: true, tsconfigRootDir: import.meta.dirname },
    },
    rules: {
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  },
  { ignores: ['dist/**', 'node_modules/**'] },
);
```

### 7.4 ESLint（前端 admin-web/eslint.config.mjs）

```javascript
import eslint from '@eslint/js';
import tseslint from 'typescript-eslint';
import reactHooks from 'eslint-plugin-react-hooks';
import reactRefresh from 'eslint-plugin-react-refresh';

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
  {
    plugins: { 'react-hooks': reactHooks, 'react-refresh': reactRefresh },
    rules: {
      ...reactHooks.configs.recommended.rules,
      'react-refresh/only-export-components': ['warn', { allowConstantExport: true }],
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  },
  { ignores: ['dist/**'] },
);
```

---

## 8. 附录

### 8.1 常见问题

**后端路由冲突：** 静态路由放在 `@Get(':id')` 之前。

**前端 401 循环：** 刷新 Token 请求本身须排除在重试逻辑外，并加请求锁防止并发刷新。

**跨域：** 后端 `CORS_ORIGINS` 包含前端 dev 地址 `http://localhost:5173`。

### 8.2 PR 审查 Checklist

**通用：**

- [ ] 提交信息符合 commitlint 格式
- [ ] 无硬编码密钥、无 `.env` 敏感文件入库
- [ ] CI（lint、test、build）通过

**后端：**

- [ ] DTO 校验 + Swagger 注解完整
- [ ] 实体继承 `BaseEntity`，变更含迁移文件
- [ ] `@RequirePermissions` / `@Public` 正确标注
- [ ] 静态路由在动态路由之前
- [ ] Service 单元测试覆盖核心逻辑

**前端：**

- [ ] API 调用经 `api/` 层，`pages/` 与 `components/` 均无裸 `axios`
- [ ] Axios 已配置 `withCredentials: true`；Refresh Token 未写入 Store
- [ ] 列表页遵循搜索 + 表格 + 弹窗 CRUD 模式
- [ ] 操作按钮已用 `AuthButton` 包裹
- [ ] 类型定义与后端响应一致（camelCase）
- [ ] 无 `any` 滥用，Props 有类型定义

**联调：**

- [ ] 权限码与后端、菜单配置三方一致
- [ ] 分页参数与响应结构符合 §2.2 约定
- [ ] 401 刷新与并发刷新场景已手动验证
- [ ] 涉及菜单/权限的变更已同步 seed 或初始化脚本
