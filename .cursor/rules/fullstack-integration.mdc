---
description: 前后端联调、Token 与 PR 自检要点
globs: server/**/*.ts
alwaysApply: false
---

# 全栈联调

## Token（唯一方案）

- Access Token：前端 `sessionStorage` + `Authorization: Bearer`
- Refresh Token：后端 `HttpOnly` Cookie；Axios `withCredentials: true`
- 刷新：`POST /api/auth/refresh` 排除重试锁；失败则 `logout()` → `/login`

## 开发顺序

1. 后端接口 + Swagger 自测通过
2. 前端 `api/` + `types/` 对接
3. 联调权限：菜单树、按钮码与 `@RequirePermissions` 三方一致

## 接口变更

后端先改 DTO/Swagger → PR 标注破坏性变更 → 前端同步 `types/` 与 `api/` → 同步菜单 seed。

## PR 自检（节选）

- [ ] 分页/响应格式符合 `前后端开发规范.md` §2.2
- [ ] 新接口有 `@RequirePermissions` 或 `@Public`
- [ ] 前端操作按钮已包 `AuthButton`；无裸 `axios`
- [ ] 401 并发刷新已验证；菜单/权限变更已更新 seed
